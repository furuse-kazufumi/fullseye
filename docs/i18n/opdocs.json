{
 "_": "Fullseye op ドキュメント/ヘルプの『枠』の対訳表。**キーは原文(日本語)** —— studio_assets/i18n.json の 'strings' と同じ規約で、翻訳者はキーを発明しなくてよく、原文を書き換えれば訳が自動的に『未訳』になる(古い訳が黙って残らない)。言語の追加は各行に 1 列足すだけで、tools/opdocs.py の変更は要らない。未訳は原文のまま出て、生成時に stderr に件数が出る。 台湾向けの繁体字は BCP 47 の zh-TW だが、コードは 2 文字にそろえて tw(対応表は opdocs.LANG_ALIASES)。",
 "languages": {
  "ja": "日本語",
  "en": "English",
  "zh": "简体中文",
  "tw": "繁體中文",
  "ko": "한국어",
  "de": "Deutsch"
 },
 "strings": {
  "## ファミリ共通の入力契約(fail-closed)": {
   "en": "## Family-wide input contract (fail-closed)",
   "zh": "## 该族通用的输入契约(fail-closed)",
   "tw": "## 該族通用的輸入契約(fail-closed)",
   "ko": "## 패밀리 공통 입력 계약(fail-closed)",
   "de": "## Familienweiter Eingabevertrag (fail-closed)"
  },
  "## 使い方": {
   "en": "## Usage",
   "zh": "## 用法",
   "tw": "## 用法",
   "ko": "## 사용법",
   "de": "## Verwendung"
  },
  "## 参考(サンプルデータ・文献)": {
   "en": "## References (sample data, literature)",
   "zh": "## 参考(示例数据・文献)",
   "tw": "## 參考(範例資料・文獻)",
   "ko": "## 참고(샘플 데이터·문헌)",
   "de": "## Referenzen (Beispieldaten, Literatur)"
  },
  "## 同カテゴリ(`{a0}`)": {
   "en": "## Same category (`{a0}`)",
   "zh": "## 同类别(`{a0}`)",
   "tw": "## 同類別(`{a0}`)",
   "ko": "## 같은 카테고리(`{a0}`)",
   "de": "## Gleiche Kategorie (`{a0}`)"
  },
  "## 型が繋がる次の op(`{a0}` を入力に取れる)": {
   "en": "## Ops the type connects to (they accept `{a0}` as input)",
   "zh": "## 类型可衔接的下一个算子(可接受 `{a0}` 作为输入)",
   "tw": "## 型別可銜接的下一個運算子(可接受 `{a0}` 作為輸入)",
   "ko": "## 타입이 이어지는 다음 연산자(`{a0}` 를 입력으로 받는 것)",
   "de": "## Typkompatible Folge-Operatoren (nehmen `{a0}` als Eingabe)"
  },
  "## 実行できる例(この op を実際に呼ぶ検証済みサンプル)": {
   "en": "## Runnable examples (verified samples that actually call this op)",
   "zh": "## 可运行的示例(实际调用该算子并已验证的样例)",
   "tw": "## 可執行的範例(實際呼叫該運算子並已驗證的樣例)",
   "ko": "## 실행 가능한 예제(이 연산자를 실제로 호출하는 검증된 샘플)",
   "de": "## Ausführbare Beispiele (verifizierte Samples, die diesen Operator wirklich aufrufen)"
  },
  "## 背景知識ガイド(この op の手前にある物理・規約)": {
   "en": "## Background guides (the physics and conventions behind this op)",
   "zh": "## 背景知识指南(该算子背后的物理与约定)",
   "tw": "## 背景知識指南(該運算子背後的物理與規範)",
   "ko": "## 배경 지식 가이드(이 연산자 앞에 있는 물리·규약)",
   "de": "## Hintergrundwissen (Physik und Konventionen hinter diesem Operator)"
  },
  "## 詳しい使い方ガイド": {
   "en": "## Detailed usage guide",
   "zh": "## 详细使用指南",
   "tw": "## 詳細使用指南",
   "ko": "## 자세한 사용 가이드",
   "de": "## Ausführlicher Anwendungsleitfaden"
  },
  "*Provenance: {a0}.py — {a1} operator registry. この per-op ノートは `tools/opdocs.py md` が自動生成(手編集しない)。*": {
   "en": "*Provenance: {a0}.py — {a1} operator registry. This per-op note is generated by `tools/opdocs.py md` (do not hand-edit).*",
   "zh": "*Provenance: {a0}.py — {a1} 算子登记表。本条目由 `tools/opdocs.py md` 自动生成(请勿手工编辑)。*",
   "tw": "*Provenance: {a0}.py — {a1} 運算子登記表。本條目由 `tools/opdocs.py md` 自動產生(請勿手動編輯)。*",
   "ko": "*Provenance: {a0}.py — {a1} 연산자 레지스트리. 이 op 노트는 `tools/opdocs.py md` 가 자동 생성합니다(직접 편집하지 마세요).*",
   "de": "*Provenance: {a0}.py — {a1} Operator-Registry. Diese Notiz wird von `tools/opdocs.py md` erzeugt (nicht von Hand bearbeiten).*"
  },
  "- (まだありません)": {
   "en": "- (none yet)",
   "zh": "- (暂无)",
   "tw": "- (尚無)",
   "ko": "- (아직 없음)",
   "de": "- (noch keine)"
  },
  "- **0 除算とその親戚を名指しで拒否**: 焦点距離 0・曲率半径 0・屈折率 <= 0・不透明な開口(全 0 なので正規化が 0/0)・総和 <= 0 の PSF・S0 = 0 の Stokes ベクトル・物体が前側焦点にある(像が無限遠)。": {
   "en": "- **Division by zero and its relatives are refused by name**: focal length 0, radius of curvature 0, refractive index <= 0, a fully opaque aperture (all zeros, so the normalisation is 0/0), a PSF whose sum is <= 0, a Stokes vector with S0 = 0, and an object sitting at the front focal point (the image is at infinity).",
   "zh": "- **逐项点名拒绝除零及其近亲**:焦距 0、曲率半径 0、折射率 <= 0、全不透明光阑(全为 0,归一化变成 0/0)、总和 <= 0 的 PSF、S0 = 0 的 Stokes 矢量、物体位于前焦点(像在无穷远)。",
   "tw": "- **逐項點名拒絕除零及其近親**:焦距 0、曲率半徑 0、折射率 <= 0、全不透明光闌(全為 0,正規化變成 0/0)、總和 <= 0 的 PSF、S0 = 0 的 Stokes 向量、物體位於前焦點(像在無窮遠)。",
   "ko": "- **0 나눗셈과 그 친척들을 이름을 들어 거부**: 초점거리 0, 곡률반경 0, 굴절률 <= 0, 불투명한 조리개(전부 0 이라 정규화가 0/0), 총합 <= 0 인 PSF, S0 = 0 인 스토크스 벡터, 물체가 앞쪽 초점에 있는 경우(상이 무한대).",
   "de": "- **Division durch null und Verwandtes wird namentlich abgelehnt**: Brennweite 0, Krümmungsradius 0, Brechzahl <= 0, undurchlässige Blende (alles 0, die Normierung wird 0/0), PSF mit Summe <= 0, Stokes-Vektor mit S0 = 0 und ein Objekt im vorderen Brennpunkt (Bild im Unendlichen)."
  },
  "- **GPU**: この op は GPU 経路あり(`device=\"cuda\"`)": {
   "en": "- **GPU**: this op has a GPU path (`device=\"cuda\"`)",
   "zh": "- **GPU**:该算子有 GPU 路径(`device=\"cuda\"`)",
   "tw": "- **GPU**:該運算子有 GPU 路徑(`device=\"cuda\"`)",
   "ko": "- **GPU**: 이 연산자는 GPU 경로가 있습니다(`device=\"cuda\"`)",
   "de": "- **GPU**: Dieser Operator hat einen GPU-Pfad (`device=\"cuda\"`)"
  },
  "- **HALCON 相当**: `{a0}`(意味・パラメータは HALCON リファレンスが参考になる)": {
   "en": "- **HALCON equivalent**: `{a0}` (the HALCON reference is a useful guide to its meaning and parameters)",
   "zh": "- **HALCON 对应**:`{a0}`(其含义与参数可参考 HALCON 手册)",
   "tw": "- **HALCON 對應**:`{a0}`(其含義與參數可參考 HALCON 手冊)",
   "ko": "- **HALCON 대응**: `{a0}`(의미·매개변수는 HALCON 레퍼런스가 참고가 됩니다)",
   "de": "- **HALCON-Äquivalent**: `{a0}` (Bedeutung und Parameter sind in der HALCON-Referenz nachzulesen)"
  },
  "- **NaN/Inf は全入力で `ValueError`**(件数を明示して拒否 — 結果全体に伝播するため)。": {
   "en": "- **NaN/Inf raises `ValueError` on every input** (refused with the count stated — it propagates through the whole result).",
   "zh": "- **所有输入中的 NaN/Inf 一律 `ValueError`**(明确给出个数后拒绝 —— 它会污染整个结果)。",
   "tw": "- **所有輸入中的 NaN/Inf 一律 `ValueError`**(明確給出個數後拒絕 —— 它會汙染整個結果)。",
   "ko": "- **NaN/Inf 는 모든 입력에서 `ValueError`**(개수를 명시하고 거부 —— 결과 전체로 전파되므로).",
   "de": "- **NaN/Inf löst bei jeder Eingabe `ValueError` aus** (mit Anzahl abgelehnt — es pflanzt sich durch das gesamte Ergebnis fort)."
  },
  "- **complex / masked array は `ValueError`**(実数枠のみ。虚部の無言切り捨て・マスク剥がしを拒否)。**NaN/Inf は全入力で `ValueError`**。": {
   "en": "- **complex / masked arrays raise `ValueError`** (real-valued slots only; silently dropping the imaginary part or peeling off the mask is refused). **NaN/Inf raises `ValueError` on every input.**",
   "zh": "- **complex / masked array 一律 `ValueError`**(仅接受实数槽位;拒绝无声丢弃虚部或剥掉掩码)。**所有输入中的 NaN/Inf 一律 `ValueError`**。",
   "tw": "- **complex / masked array 一律 `ValueError`**(僅接受實數槽位;拒絕無聲丟棄虛部或剝掉遮罩)。**所有輸入中的 NaN/Inf 一律 `ValueError`**。",
   "ko": "- **complex / masked array 는 `ValueError`**(실수 슬롯만. 허수부의 무언 절삭·마스크 벗기기를 거부).**NaN/Inf 는 모든 입력에서 `ValueError`**.",
   "de": "- **complex / Masked Arrays lösen `ValueError` aus** (nur reelle Slots; das stille Verwerfen des Imaginärteils bzw. Abstreifen der Maske wird abgelehnt). **NaN/Inf löst bei jeder Eingabe `ValueError` aus.**"
  },
  "- **complex 入力は `ValueError`** — float64 への強制変換は虚部を黙って捨てる(numpy は ComplexWarning だけ出して「もっともらしく間違った」実数を返す)。`.real`/`.imag`/`abs()` を明示するか、複素対応の complexops を使う。": {
   "en": "- **complex input raises `ValueError`** — coercing to float64 silently discards the imaginary part (numpy only emits a ComplexWarning and returns a plausible-looking wrong real number). State `.real`/`.imag`/`abs()` explicitly, or use complexops, which handles complex data.",
   "zh": "- **complex 输入一律 `ValueError`** —— 强制转成 float64 会无声丢掉虚部(numpy 只发一个 ComplexWarning,然后返回一个「看着合理却是错的」实数)。请显式写出 `.real`/`.imag`/`abs()`,或改用支持复数的 complexops。",
   "tw": "- **complex 輸入一律 `ValueError`** —— 強制轉成 float64 會無聲丟掉虛部(numpy 只發一個 ComplexWarning,然後回傳一個「看似合理卻是錯的」實數)。請明確寫出 `.real`/`.imag`/`abs()`,或改用支援複數的 complexops。",
   "ko": "- **complex 입력은 `ValueError`** —— float64 로의 강제 변환은 허수부를 조용히 버립니다(numpy 는 ComplexWarning 만 내고 「그럴듯하게 틀린」 실수를 돌려줍니다). `.real`/`.imag`/`abs()` 를 명시하거나 복소수를 다루는 complexops 를 쓰세요.",
   "de": "- **Komplexe Eingaben lösen `ValueError` aus** — die Umwandlung nach float64 verwirft den Imaginärteil stillschweigend (numpy meldet nur eine ComplexWarning und liefert eine plausibel aussehende, falsche reelle Zahl). Schreiben Sie `.real`/`.imag`/`abs()` explizit oder nutzen Sie complexops."
  },
  "- **masked array(masked 要素あり)は `ValueError`** — マスクを剥がして下の生値を使う暗黙変換を拒否。埋める/落とすを明示する。": {
   "en": "- **masked arrays with masked elements raise `ValueError`** — the implicit conversion that peels off the mask and uses the raw values underneath is refused. Say explicitly whether to fill or to drop.",
   "zh": "- **含被掩元素的 masked array 一律 `ValueError`** —— 拒绝「剥掉掩码直接使用下面原值」的隐式转换。请显式选择填充还是丢弃。",
   "tw": "- **含被遮罩元素的 masked array 一律 `ValueError`** —— 拒絕「剝掉遮罩直接使用下面原值」的隱式轉換。請明確選擇填補還是丟棄。",
   "ko": "- **masked 요소가 있는 masked array 는 `ValueError`** —— 마스크를 벗겨 아래 원값을 쓰는 암묵 변환을 거부합니다. 채울지 버릴지를 명시하세요.",
   "de": "- **Masked Arrays mit maskierten Elementen lösen `ValueError` aus** — die implizite Umwandlung, die die Maske abstreift und die Rohwerte darunter verwendet, wird abgelehnt. Geben Sie explizit an, ob gefüllt oder verworfen wird."
  },
  "- **サイズ上限**: 生成格子は `optics.MAX_GRID`(4096)、供給された場/PSF/開口は `optics.MAX_FIELD_ELEMENTS`(2^24)、ABCD 素子列は `optics.MAX_SYSTEM_ELEMENTS`(1024)、Zernike は `MAX_ZERNIKE_TERMS`(512)/ `MAX_ZERNIKE_ORDER`(40)/ `MAX_ZERNIKE_BASIS`(2^25)。小さな引数から巨大な内部確保が起きる経路(実測: n_max=40 × 4096² で 108 GB)を fail-closed で塞ぐ。": {
   "en": "- **Size caps**: generated grids are capped by `optics.MAX_GRID` (4096); supplied fields/PSFs/apertures by `optics.MAX_FIELD_ELEMENTS` (2^24); ABCD element chains by `optics.MAX_SYSTEM_ELEMENTS` (1024); Zernike by `MAX_ZERNIKE_TERMS` (512) / `MAX_ZERNIKE_ORDER` (40) / `MAX_ZERNIKE_BASIS` (2^25). This closes, fail-closed, the paths where a small argument triggers a huge internal allocation (measured: n_max=40 × 4096² needs 108 GB).",
   "zh": "- **尺寸上限**:生成网格受 `optics.MAX_GRID`(4096)限制,传入的场/PSF/光阑受 `optics.MAX_FIELD_ELEMENTS`(2^24),ABCD 元件序列受 `optics.MAX_SYSTEM_ELEMENTS`(1024),Zernike 受 `MAX_ZERNIKE_TERMS`(512)/ `MAX_ZERNIKE_ORDER`(40)/ `MAX_ZERNIKE_BASIS`(2^25)。以 fail-closed 堵住「小参数引发巨大内部分配」的路径(实测:n_max=40 × 4096² 需要 108 GB)。",
   "tw": "- **尺寸上限**:生成網格受 `optics.MAX_GRID`(4096)限制,傳入的場/PSF/光闌受 `optics.MAX_FIELD_ELEMENTS`(2^24),ABCD 元件序列受 `optics.MAX_SYSTEM_ELEMENTS`(1024),Zernike 受 `MAX_ZERNIKE_TERMS`(512)/ `MAX_ZERNIKE_ORDER`(40)/ `MAX_ZERNIKE_BASIS`(2^25)。以 fail-closed 堵住「小參數引發巨大內部配置」的路徑(實測:n_max=40 × 4096² 需要 108 GB)。",
   "ko": "- **크기 상한**: 생성 격자는 `optics.MAX_GRID`(4096), 넘겨받은 필드/PSF/조리개는 `optics.MAX_FIELD_ELEMENTS`(2^24), ABCD 소자열은 `optics.MAX_SYSTEM_ELEMENTS`(1024), Zernike 는 `MAX_ZERNIKE_TERMS`(512)/ `MAX_ZERNIKE_ORDER`(40)/ `MAX_ZERNIKE_BASIS`(2^25). 작은 인자에서 거대한 내부 할당이 일어나는 경로(실측: n_max=40 × 4096² 로 108 GB)를 fail-closed 로 막습니다.",
   "de": "- **Größenobergrenzen**: erzeugte Gitter durch `optics.MAX_GRID` (4096), übergebene Felder/PSFs/Blenden durch `optics.MAX_FIELD_ELEMENTS` (2^24), ABCD-Elementketten durch `optics.MAX_SYSTEM_ELEMENTS` (1024), Zernike durch `MAX_ZERNIKE_TERMS` (512) / `MAX_ZERNIKE_ORDER` (40) / `MAX_ZERNIKE_BASIS` (2^25). Damit werden Pfade fail-closed geschlossen, in denen ein kleines Argument eine riesige interne Allokation auslöst (gemessen: n_max=40 × 4096² braucht 108 GB)."
  },
  "- **サイズ上限**: 行列を取る op と `stat_histogram` の bins は `mathops.MAX_ELEMENTS`(2^26 ≈ 6700 万要素)超で `ValueError`。": {
   "en": "- **Size cap**: ops that take a matrix, and the `stat_histogram` bins, raise `ValueError` beyond `mathops.MAX_ELEMENTS` (2^26 ≈ 67 million elements).",
   "zh": "- **尺寸上限**:接受矩阵的算子与 `stat_histogram` 的 bins,超过 `mathops.MAX_ELEMENTS`(2^26 ≈ 6700 万个元素)即 `ValueError`。",
   "tw": "- **尺寸上限**:接受矩陣的運算子與 `stat_histogram` 的 bins,超過 `mathops.MAX_ELEMENTS`(2^26 ≈ 6700 萬個元素)即 `ValueError`。",
   "ko": "- **크기 상한**: 행렬을 받는 연산자와 `stat_histogram` 의 bins 는 `mathops.MAX_ELEMENTS`(2^26 ≈ 6700 만 요소)를 넘으면 `ValueError`.",
   "de": "- **Größenobergrenze**: Operatoren, die eine Matrix nehmen, sowie die `stat_histogram`-Bins lösen jenseits von `mathops.MAX_ELEMENTS` (2^26 ≈ 67 Mio. Elemente) `ValueError` aus."
  },
  "- **データ種**: `{a0}` → `{a1}`": {
   "en": "- **Data kinds**: `{a0}` → `{a1}`",
   "zh": "- **数据种类**:`{a0}` → `{a1}`",
   "tw": "- **資料種類**:`{a0}` → `{a1}`",
   "ko": "- **데이터 종류**: `{a0}` → `{a1}`",
   "de": "- **Datenarten**: `{a0}` → `{a1}`"
  },
  "- **単位は引数名に埋め込む** — `_mm` / `_um` / `_deg` / `_mrad`。mm と µm の取り違えは crash ではなく「もっともらしく間違った答え」なので、名前で防ぐ。大きさから単位を推測する処理は一切しない。": {
   "en": "- **Units are baked into the argument name** — `_mm` / `_um` / `_deg` / `_mrad`. Confusing mm with µm does not crash; it yields a plausible-looking wrong answer, so the name prevents it. Nothing here guesses the unit from the magnitude.",
   "zh": "- **单位写进参数名** —— `_mm` / `_um` / `_deg` / `_mrad`。把 mm 和 µm 弄混不会崩溃,而是给出「看着合理却是错的答案」,所以用命名来防。这里绝不从数值大小去猜单位。",
   "tw": "- **單位寫進參數名** —— `_mm` / `_um` / `_deg` / `_mrad`。把 mm 和 µm 弄混不會當掉,而是給出「看似合理卻是錯的答案」,所以用命名來防。這裡絕不從數值大小去猜單位。",
   "ko": "- **단위는 인자 이름에 넣습니다** — `_mm` / `_um` / `_deg` / `_mrad`. mm 와 µm 를 혼동하면 crash 가 아니라 「그럴듯하게 틀린 답」이 나오므로 이름으로 막습니다. 크기로 단위를 추측하는 처리는 일절 하지 않습니다.",
   "de": "- **Einheiten stecken im Argumentnamen** — `_mm` / `_um` / `_deg` / `_mrad`. Eine Verwechslung von mm und µm stürzt nicht ab, sondern liefert eine plausibel aussehende falsche Antwort; der Name verhindert das. Aus der Größenordnung wird nie auf die Einheit geschlossen."
  },
  "- **呼び出し**: `fullseye.apply(img, \"{a0}\", a=0.5, b=0.5)` (2-D は 1 画像 + 2 スカラつまみ `a,b∈[0,1]` のモデル)": {
   "en": "- **Call**: `fullseye.apply(img, \"{a0}\", a=0.5, b=0.5)` (the 2-D model is one image plus two scalar knobs `a,b∈[0,1]`)",
   "zh": "- **调用**:`fullseye.apply(img, \"{a0}\", a=0.5, b=0.5)`(2-D 的模型是一张图 + 两个标量旋钮 `a,b∈[0,1]`)",
   "tw": "- **呼叫**:`fullseye.apply(img, \"{a0}\", a=0.5, b=0.5)`(2-D 的模型是一張圖 + 兩個純量旋鈕 `a,b∈[0,1]`)",
   "ko": "- **호출**: `fullseye.apply(img, \"{a0}\", a=0.5, b=0.5)`(2-D 는 이미지 1 장 + 스칼라 노브 2 개 `a,b∈[0,1]` 모델)",
   "de": "- **Aufruf**: `fullseye.apply(img, \"{a0}\", a=0.5, b=0.5)` (das 2-D-Modell ist ein Bild plus zwei skalare Regler `a,b∈[0,1]`)"
  },
  "- **呼び出し**: `import {a0}; {a1}.{a2}{a3}` (または `{a4}.get(\"{a5}\")`)": {
   "en": "- **Call**: `import {a0}; {a1}.{a2}{a3}` (or `{a4}.get(\"{a5}\")`)",
   "zh": "- **调用**:`import {a0}; {a1}.{a2}{a3}`(或 `{a4}.get(\"{a5}\")`)",
   "tw": "- **呼叫**:`import {a0}; {a1}.{a2}{a3}`(或 `{a4}.get(\"{a5}\")`)",
   "ko": "- **호출**: `import {a0}; {a1}.{a2}{a3}`(또는 `{a4}.get(\"{a5}\")`)",
   "de": "- **Aufruf**: `import {a0}; {a1}.{a2}{a3}` (oder `{a4}.get(\"{a5}\")`)"
  },
  "- **形状は厳格**: 1-D と 2-D を暗黙昇格・ブロードキャストしない(vector 枠に matrix、matrix 枠に vector は `ValueError`。reshape を明示する)。": {
   "en": "- **Shapes are strict**: 1-D and 2-D are never implicitly promoted or broadcast (a matrix in a vector slot, or a vector in a matrix slot, raises `ValueError`; reshape explicitly).",
   "zh": "- **形状严格**:不对 1-D 与 2-D 做隐式提升或广播(向量槽位收到矩阵、矩阵槽位收到向量都是 `ValueError`;请显式 reshape)。",
   "tw": "- **形狀嚴格**:不對 1-D 與 2-D 做隱式提升或廣播(向量槽位收到矩陣、矩陣槽位收到向量都是 `ValueError`;請明確 reshape)。",
   "ko": "- **형상은 엄격**: 1-D 와 2-D 를 암묵적으로 승격·브로드캐스트하지 않습니다(vector 슬롯에 matrix, matrix 슬롯에 vector 는 `ValueError`. reshape 를 명시하세요).",
   "de": "- **Formen sind strikt**: 1-D und 2-D werden nie implizit hochgestuft oder gebroadcastet (eine Matrix im Vektor-Slot oder ein Vektor im Matrix-Slot löst `ValueError` aus; reshapen Sie explizit)."
  },
  "- **文字列は `ValueError`** — `float('50')` は成功してしまうため、未パースの設定値が長さとして通り抜ける(実測: `thin_lens('50', '200')` がもっともらしい 66.667 mm を返していた)。bool も `True == 1` の暗黙昇格として拒否。": {
   "en": "- **Strings raise `ValueError`** — `float('50')` succeeds, so an unparsed configuration value would slip through as a length (measured: `thin_lens('50', '200')` returned a plausible 66.667 mm). bool is refused too, as the implicit promotion `True == 1`.",
   "zh": "- **字符串一律 `ValueError`** —— `float('50')` 会成功,于是未解析的配置值会被当成长度混进来(实测:`thin_lens('50', '200')` 曾返回看着合理的 66.667 mm)。bool 也按 `True == 1` 的隐式提升拒绝。",
   "tw": "- **字串一律 `ValueError`** —— `float('50')` 會成功,於是未解析的設定值會被當成長度混進來(實測:`thin_lens('50', '200')` 曾回傳看似合理的 66.667 mm)。bool 也按 `True == 1` 的隱式提升拒絕。",
   "ko": "- **문자열은 `ValueError`** —— `float('50')` 은 성공하므로 파싱되지 않은 설정값이 길이로 통과해 버립니다(실측: `thin_lens('50', '200')` 이 그럴듯한 66.667 mm 를 돌려주고 있었습니다). bool 도 `True == 1` 의 암묵 승격으로 보고 거부합니다.",
   "de": "- **Strings lösen `ValueError` aus** — `float('50')` gelingt, sodass ein ungeparster Konfigurationswert als Länge durchrutschen würde (gemessen: `thin_lens('50', '200')` lieferte plausible 66,667 mm). bool wird als implizite Hochstufung `True == 1` ebenfalls abgelehnt."
  },
  "- **物理的に不可能な状態も拒否**: 偏光度 > 1 の Stokes ベクトル、負の透過率、負の強度、n-|m| が奇数などの不正な Zernike 添字。": {
   "en": "- **Physically impossible states are refused too**: a Stokes vector with degree of polarisation > 1, negative transmittance, negative intensity, and invalid Zernike indices such as n-|m| odd.",
   "zh": "- **物理上不可能的状态同样拒绝**:偏振度 > 1 的 Stokes 矢量、负透过率、负强度、n-|m| 为奇数等非法 Zernike 指标。",
   "tw": "- **物理上不可能的狀態同樣拒絕**:偏振度 > 1 的 Stokes 向量、負穿透率、負強度、n-|m| 為奇數等非法 Zernike 指標。",
   "ko": "- **물리적으로 불가능한 상태도 거부**: 편광도 > 1 인 스토크스 벡터, 음의 투과율, 음의 강도, n-|m| 이 홀수인 잘못된 Zernike 첨자 등.",
   "de": "- **Physikalisch unmögliche Zustände werden ebenfalls abgelehnt**: Stokes-Vektor mit Polarisationsgrad > 1, negative Transmission, negative Intensität und ungültige Zernike-Indizes wie ungerades n-|m|."
  },
  "- **非有限を返すのは 2 op だけ、しかも契約として明記**: `depth_of_field` の過焦点距離以遠の `far_mm = inf`(それが過焦点距離の定義)と `gaussian_beam` のウエストでの `wavefront_radius_mm = inf`(平面波面の曲率半径)。どちらも有限の相棒(`far_is_infinite` / `curvature_per_mm`)を併せて返す。**それ以外の無言 NaN/Inf は内部で検出して `ValueError`** —「float64 が溢れた」と「答えが無限大」は別の主張なので、後者の顔で前者を返さない。": {
   "en": "- **Only two ops return a non-finite value, and both state it as a contract**: `depth_of_field` returns `far_mm = inf` beyond the hyperfocal distance (that is what the hyperfocal distance means), and `gaussian_beam` returns `wavefront_radius_mm = inf` at the waist (the radius of curvature of a plane wavefront). Both also return a finite companion (`far_is_infinite` / `curvature_per_mm`). **Any other silent NaN/Inf is detected internally and raises `ValueError`** — \"float64 overflowed\" and \"the answer is infinite\" are different claims, so the first is never returned wearing the face of the second.",
   "zh": "- **只有两个算子会返回非有限值,而且都写进了契约**:`depth_of_field` 在超焦距以外返回 `far_mm = inf`(这正是超焦距的定义),`gaussian_beam` 在束腰处返回 `wavefront_radius_mm = inf`(平面波前的曲率半径)。两者都同时返回一个有限的搭档(`far_is_infinite` / `curvature_per_mm`)。**除此之外的无声 NaN/Inf 都在内部检出并 `ValueError`** ——「float64 溢出了」和「答案是无穷大」是两种不同的主张,不能拿后者的脸去交付前者。",
   "tw": "- **只有兩個運算子會回傳非有限值,而且都寫進了契約**:`depth_of_field` 在超焦距以外回傳 `far_mm = inf`(這正是超焦距的定義),`gaussian_beam` 在束腰處回傳 `wavefront_radius_mm = inf`(平面波前的曲率半徑)。兩者都同時回傳一個有限的夥伴(`far_is_infinite` / `curvature_per_mm`)。**除此之外的無聲 NaN/Inf 都在內部檢出並 `ValueError`** ——「float64 溢位了」和「答案是無窮大」是兩種不同的主張,不能拿後者的臉去交付前者。",
   "ko": "- **비유한 값을 돌려주는 것은 2 개 연산자뿐이며, 둘 다 계약으로 명시**: `depth_of_field` 의 과초점거리 너머 `far_mm = inf`(그것이 과초점거리의 정의)와 `gaussian_beam` 의 허리에서의 `wavefront_radius_mm = inf`(평면 파면의 곡률반경). 둘 다 유한한 짝(`far_is_infinite` / `curvature_per_mm`)을 함께 돌려줍니다. **그 밖의 무언 NaN/Inf 는 내부에서 검출해 `ValueError`** —— 「float64 가 넘쳤다」와 「답이 무한대다」는 다른 주장이므로, 후자의 얼굴로 전자를 돌려주지 않습니다.",
   "de": "- **Nur zwei Operatoren liefern einen nicht-endlichen Wert, und beide halten das vertraglich fest**: `depth_of_field` liefert jenseits der hyperfokalen Distanz `far_mm = inf` (genau das bedeutet die hyperfokale Distanz), und `gaussian_beam` liefert an der Taille `wavefront_radius_mm = inf` (der Krümmungsradius einer ebenen Wellenfront). Beide liefern zusätzlich einen endlichen Partner (`far_is_infinite` / `curvature_per_mm`). **Jedes andere stille NaN/Inf wird intern erkannt und löst `ValueError` aus** — \"float64 ist übergelaufen\" und \"die Antwort ist unendlich\" sind verschiedene Aussagen; die erste wird nie im Gewand der zweiten geliefert."
  },
  "- [{a0} ファミリ ガイド]({a1})": {
   "en": "- [{a0} family guide]({a1})",
   "zh": "- [{a0} 族使用指南]({a1})",
   "tw": "- [{a0} 族使用指南]({a1})",
   "ko": "- [{a0} 패밀리 가이드]({a1})",
   "de": "- [Leitfaden zur Familie {a0}]({a1})"
  },
  "- [サンプルデータ カタログ(DL URL / ライセンス)]({a0}) — 2-D は skimage.data(BSD/public)+ 合成、3-D は実データ源(Stanford/PDS 等)の DL URL。": {
   "en": "- [Sample-data catalog (download URLs / licences)]({a0}) — 2-D uses skimage.data (BSD/public domain) plus synthetic images; 3-D lists download URLs for real data sources (Stanford, PDS, …).",
   "zh": "- [示例数据目录(下载 URL / 许可证)]({a0}) —— 2-D 用 skimage.data(BSD/公有领域)加合成图,3-D 给出真实数据源(Stanford/PDS 等)的下载 URL。",
   "tw": "- [範例資料目錄(下載 URL / 授權)]({a0}) —— 2-D 用 skimage.data(BSD/公有領域)加合成圖,3-D 給出真實資料源(Stanford/PDS 等)的下載 URL。",
   "ko": "- [샘플 데이터 카탈로그(DL URL / 라이선스)]({a0}) —— 2-D 는 skimage.data(BSD/public)+ 합성, 3-D 는 실데이터 소스(Stanford/PDS 등)의 DL URL.",
   "de": "- [Katalog der Beispieldaten (Download-URLs / Lizenzen)]({a0}) — 2-D nutzt skimage.data (BSD/Public Domain) plus synthetische Bilder, 3-D nennt Download-URLs echter Datenquellen (Stanford, PDS, …)."
  },
  "- [演算子の来歴・参考文献]({a0}) — この op 族の元になった研究/手法の出典。": {
   "en": "- [Operator provenance and references]({a0}) — the sources of the research/methods this op family came from.",
   "zh": "- [算子来历与参考文献]({a0}) —— 该算子族所依据的研究/方法出处。",
   "tw": "- [運算子來歷與參考文獻]({a0}) —— 該運算子族所依據的研究/方法出處。",
   "ko": "- [연산자의 내력·참고문헌]({a0}) —— 이 연산자 족의 바탕이 된 연구/기법의 출처.",
   "de": "- [Herkunft und Literatur der Operatoren]({a0}) — die Quellen der Forschung/Verfahren, auf denen diese Operatorfamilie beruht."
  },
  "- アルゴリズムの正典(著者・年)と用途は上記**ファミリ使い方ガイド**に記載。": {
   "en": "- The canonical algorithm (author, year) and its uses are named in the **family usage guide** above.",
   "zh": "- 算法的正典(作者・年份)与用途见上面的**族使用指南**。",
   "tw": "- 演算法的正典(作者・年份)與用途見上面的**族使用指南**。",
   "ko": "- 알고리즘의 정전(저자·연도)과 용도는 위의 **패밀리 사용 가이드**에 적혀 있습니다.",
   "de": "- Der kanonische Algorithmus (Autor, Jahr) und seine Anwendungen stehen im **Familienleitfaden** oben."
  },
  "mathops の全 op は入力を検証してから計算する(黙って通さない):": {
   "en": "Every mathops op validates its input before computing (nothing slips through silently):",
   "zh": "mathops 的每个算子都先校验输入再计算(不让任何东西无声通过):",
   "tw": "mathops 的每個運算子都先檢驗輸入再計算(不讓任何東西無聲通過):",
   "ko": "mathops 의 모든 연산자는 입력을 검증한 뒤에 계산합니다(조용히 통과시키지 않습니다):",
   "de": "Jeder mathops-Operator prüft seine Eingabe vor der Berechnung (nichts rutscht stillschweigend durch):"
  },
  "optics の全 op は入力を検証してから計算する(黙って通さない):": {
   "en": "Every optics op validates its input before computing (nothing slips through silently):",
   "zh": "optics 的每个算子都先校验输入再计算(不让任何东西无声通过):",
   "tw": "optics 的每個運算子都先檢驗輸入再計算(不讓任何東西無聲通過):",
   "ko": "optics 의 모든 연산자는 입력을 검증한 뒤에 계산합니다(조용히 통과시키지 않습니다):",
   "de": "Jeder optics-Operator prüft seine Eingabe vor der Berechnung (nichts rutscht stillschweigend durch):"
  },
  "型契約は `{a0} → {a1}`。挙動の言語説明は下記のファミリ使い方ガイドと実行可能サンプルを参照(ここでは推測を書かない)。": {
   "en": "The type contract is `{a0} → {a1}`. For what it does in words, see the family usage guide and the runnable samples below (nothing is guessed here).",
   "zh": "类型契约为 `{a0} → {a1}`。行为的文字说明请见下面的族使用指南与可运行样例(这里不做臆测)。",
   "tw": "型別契約為 `{a0} → {a1}`。行為的文字說明請見下面的族使用指南與可執行樣例(這裡不做臆測)。",
   "ko": "타입 계약은 `{a0} → {a1}`. 동작의 언어 설명은 아래 패밀리 사용 가이드와 실행 가능한 샘플을 보세요(여기서는 추측을 쓰지 않습니다).",
   "de": "Der Typvertrag lautet `{a0} → {a1}`. Was der Operator in Worten tut, steht im Familienleitfaden und in den ausführbaren Beispielen unten (hier wird nichts geraten)."
  },
  "- **上書き登録**: この名前は 2 回登録されている(core 実装 + backend の安全ラッパ)。`apply` が実行するのは**後勝ちの安全版**(fail-closed ラッパ)。core 版は backend 不在時のフォールバックとして残る(登録順=Wave0 stable slot は不変、`tests/test_opdocs.py` が上書き集合を pin)。": {
   "en": "- **Overriding registration**: this name is registered twice (the core implementation plus the backend's safe wrapper). What `apply` runs is the **last-one-wins safe version** (the fail-closed wrapper); the core version stays as the fallback when the backend is absent (registration order = the Wave0 stable slot is unchanged, and `tests/test_opdocs.py` pins the override set).",
   "zh": "- **覆盖注册**:该名字注册了两次(core 实现 + backend 的安全包装)。`apply` 实际执行的是**后注册者胜出的安全版**(fail-closed 包装);core 版作为 backend 缺席时的回退保留(注册顺序=Wave0 stable slot 不变,`tests/test_opdocs.py` 会 pin 住覆盖集合)。",
   "tw": "- **覆寫註冊**:該名字註冊了兩次(core 實作 + backend 的安全包裝)。`apply` 實際執行的是**後註冊者勝出的安全版**(fail-closed 包裝);core 版作為 backend 缺席時的後備保留(註冊順序=Wave0 stable slot 不變,`tests/test_opdocs.py` 會 pin 住覆寫集合)。",
   "ko": "- **덮어쓰기 등록**: 이 이름은 두 번 등록되어 있습니다(core 구현 + backend 의 안전 래퍼). `apply` 가 실행하는 것은 **나중에 이긴 안전판**(fail-closed 래퍼)이고, core 판은 backend 부재 시의 폴백으로 남습니다(등록 순서=Wave0 stable slot 은 불변, `tests/test_opdocs.py` 가 덮어쓰기 집합을 pin).",
   "de": "- **Überschreibende Registrierung**: Dieser Name ist zweimal registriert (Core-Implementierung plus sicherer Wrapper des Backends). `apply` führt die **zuletzt registrierte, sichere Variante** aus (den fail-closed Wrapper); die Core-Variante bleibt als Fallback, wenn das Backend fehlt (die Registrierungsreihenfolge = der Wave0-Stable-Slot bleibt unverändert, und `tests/test_opdocs.py` pinnt die Override-Menge)."
  },
  "このガイドは日本語のみです(人が書いた散文なので機械的な差し替えをしていません)。": {
   "en": "This guide is available in Japanese only (it is human-written prose, so nothing here is machine-substituted).",
   "zh": "本指南仅有日文版(它是人写的散文,这里不做机械替换)。",
   "tw": "本指南僅有日文版(它是人寫的散文,這裡不做機械替換)。",
   "ko": "이 가이드는 일본어만 있습니다(사람이 쓴 산문이라 기계적으로 치환하지 않았습니다).",
   "de": "Dieser Leitfaden liegt nur auf Japanisch vor (es ist von Menschen geschriebene Prosa, hier wird nichts maschinell ersetzt)."
  },
  "- **データ種**: `{a0}` → `{a1}`(引数だけで決まる op —— 画像やデータの入力を取らない)": {
   "en": "- **Data kinds**: `{a0}` → `{a1}` (an op determined by its arguments alone — it takes no image or data input)",
   "zh": "- **数据种类**:`{a0}` → `{a1}`(仅由参数决定的算子 —— 不接受图像或数据输入)",
   "tw": "- **資料種類**:`{a0}` → `{a1}`(僅由參數決定的運算子 —— 不接受影像或資料輸入)",
   "ko": "- **데이터 종류**: `{a0}` → `{a1}`(인자만으로 정해지는 연산자 —— 이미지나 데이터 입력을 받지 않습니다)",
   "de": "- **Datenarten**: `{a0}` → `{a1}` (ein Operator, der allein durch seine Argumente bestimmt ist — er nimmt kein Bild und keine Daten entgegen)"
  },
  "なし": {
   "en": "none",
   "zh": "无",
   "tw": "無",
   "ko": "없음",
   "de": "keine"
  },
  "> この op の説明はまだ訳がありません。原文をそのまま載せます。": {
   "en": "> This operator's description has not been translated yet. The original text follows as it is.",
   "zh": "> 该算子的说明尚无译文,以下照原文给出。",
   "tw": "> 該運算子的說明尚無譯文,以下照原文給出。",
   "ko": "> 이 연산자의 설명은 아직 번역이 없습니다. 원문을 그대로 싣습니다.",
   "de": "> Für diesen Operator gibt es noch keine Übersetzung. Es folgt der Originaltext unverändert."
  },
  "> 以下の詳細説明は原文のままです —— 要約と見出しは訳出済み。": {
   "en": "> The detailed description below is the original text — the summary and the headings are translated.",
   "zh": "> 以下的详细说明为原文 —— 摘要与标题已翻译。",
   "tw": "> 以下的詳細說明為原文 —— 摘要與標題已翻譯。",
   "ko": "> 아래 상세 설명은 원문입니다 —— 요약과 제목은 번역되어 있습니다.",
   "de": "> Die ausführliche Beschreibung unten ist der Originaltext — Zusammenfassung und Überschriften sind übersetzt."
  }
 }
}
